toolName: run_command
            
status: success
          
command: uv run python -c "
import pkgutil, importlib, braindecode.models as mod
import re
names = sorted(x.name for x in pkgutil.iter_modules(mod.__path__) if x.name != 'util' and x.name != 'config' and x.name != 'base' and x.name != 'interpolated')
for n in names:
    try:
        m = importlib.import_module('braindecode.models.' + n)
        doc = m.__doc__ or ''
        first = doc.strip().split('\n')[0] if doc.strip() else ''
        hit = 'Transformer' in doc or 'transformer' in doc
        print(f'{n:28s} transformer={hit}  | {first[:80]}')
    except Exception as e:
        print(f'{n:28s} ERROR {e}')
" undefined
          
模块 docstring 只匹配到 CTNet 和 Medformer，但很多模型是类 docstring 里才提到 Transformer。让我更彻底地检查类 docstring。


        
            
toolName: run_command
            
status: success
          
command: uv run python -c "
import pkgutil, importlib, inspect, braindecode.models as mod
import re
names = sorted(x.name for x in pkgutil.iter_modules(mod.__path__))
kw = re.compile(r'transformer|self[- ]attention|vision transformer|BERT|attention', re.I)
cands = ['attentionbasenet','attn_sleep','bendr','biot','codebrain','eegconformer','eegdino','eegpt','eegsym','labram','luna','medformer','msvtnet','mvpformer','patchedtransformer','steegformer','syncnet','tcformer','ctnet','dance','bendr','contrawr']
for n in sorted(set(cands)):
    try:
        m = importlib.import_module('braindecode.models.' + n)
        texts = [m.__doc__ or '']
        for _, cls in inspect.getmembers(m, inspect.isclass):
            if cls.__module__ == m.__name__:
                texts.append(cls.__doc__ or '')
        joined = '\n'.join(texts)
        hits = [l.strip()[:90] for l in joined.split('\n') if kw.search(l)]
        print(f'=== {n} ===')
        for h in hits[:4]: print('   ', h)
    except Exception as e:
        print(f'{n} ERROR {e}')
" undefined
          
让我核实几个边界模型（EEGSym、EEG-Miner、SyncNet 等）是否真的用到 Transformer。


        
            
toolName: run_command
            
status: success
          
command: uv run python -c "
import importlib, inspect, re
kw = re.compile(r'transformer|self[- ]attention|multi-head|attention', re.I)
for n in ['eegsym','eegminer','syncnet','eegnex','tsinception','eegsimpleconv','atcnet']:
    m = importlib.import_module('braindecode.models.' + n)
    texts = [m.__doc__ or '']
    for _, cls in inspect.getmembers(m, inspect.isclass):
        if cls.__module__ == m.__name__:
            texts.append(cls.__doc__ or '')
    joined = '\n'.join(texts)
    hits = [l.strip()[:100] for l in joined.split('\n') if kw.search(l)]
    print(f'=== {n} ===')
    for h in hits[:5]: print('   ', h)
    if not hits: print('    (no attention/transformer mention)')
" undefined
          
已核对 braindecode 1.7.0 源码，按"Transformer 的使用程度"分类如下。

## 一、以 Transformer 编码器为核心（纯/以 Transformer 为主）

| 模型 | 文献年份 | 说明 |
|------|---------|------|
| **BENDR** | Kostas 2021 | CNN 特征提取 + Transformer 编码器（Contextualizer），BERT 风格自监督预训练 |
| **BIOT** | Yang 2023 | 基于 LinearAttentionTransformer，跨物种生物信号 Transformer |
| **CodeBrain** | 2024 | 状态空间模型(S4) + 滑窗多头自注意力，SAINT 风格 |
| **ContraWR** | Yang 2023 | 生物信号 Transformer，跨数据对比学习 |
| **EEGPT** | 2024 | 通用 EEG 特征提取的预训练 Transformer（Foundation Model）|
| **EEGDINO** | 2024 | 去耦位置编码 + Transformer 编码器 + 全局 token，DINO 式自监督 |
| **LaBraM** | 2024 | BERT 式 Transformer（窗口注意力块），Foundation Model |
| **Luna** | 2024 | 通道统一(交叉注意力) + RoPE Transformer 编码器 |
| **Medformer** | 2023 | 多粒度分块 Transformer，两阶段注意力 |
| **MVPFormer** | 2024 | 多变量并行注意力 Transformer（Foundation Model）|
| **STEEGFormer** | 2024 | ViT-MAE EEG 基础模型（预归 Transformer 块）|
| **PatchedTransformer (PBT)** | Klein 2025 | Patched Brain Transformer |

## 二、卷积 + Transformer 混合（CNN 为主，注意力增强）

| 模型 | 文献年份 | 说明 |
|------|---------|------|
| **CTNet** | 2022 | 卷积 + Transformer 编码器，**运动想象** |
| **TCFormer** | 2023 | 多核卷积前端 + 分组查询 Transformer，**运动想象** |
| **EEG-Conformer** | Song 2022 | 卷积优先 + 轻量 Transformer 编码器 |
| **ATCNet** | 2022 | 卷积优先 + 多头自注意力(attention→TCN) 分支，**运动想象** |
| **MSVTNet** | 2021 | 多尺度卷积 Transformer 网络 |
| **AttentionBaseNet** | Wimpff 2023 | 卷积优先 + 通道注意力阶段 |
| **AttnSleep** | Eldele 2021 | 多头注意力时序编码器（睡眠分期）|
| **DANCE** | 2024 | DETR 式检测 Transformer（交叉注意力 Perceiver）|

## 三、明确**不是** Transformer 的（避免混淆）

- **项目现有三个模型都非 Transformer**：EEGNet、ShallowFBCSPNet、Deep4Net（均为纯 CNN）
- 名字带 attention 但实为 CNN/TCN 的：`TCN(BDTCN)`（时序卷积+注意力）、`EEG-TCNet`、`EEG-ITNet`、`EEGInception`
- 未检出注意力/Transformer 描述的：`EEGSym`、`EEG-Miner`、`SyncNet`、`EEGNex`、`TSInception` 等

## 四、对你项目的参考

你的项目是**运动想象四分类**，若后续要对比/引入 Transformer 类模型，最对口的是上面标"运动想象"的四个：**CTNet、TCFormer、EEG-Conformer、ATCNet**，它们都是"CNN 提局部特征 + Transformer 提全局时序依赖"的典型设计。如需仿照现有结构新增其中某个到 `MODEL_REGISTRY`，告诉我即可。